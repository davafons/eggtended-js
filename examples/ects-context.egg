do (
  def(x, object (
    "c", 0,
    "gc", -> { c },
    "sc", ->{value, =(c, value)},
    "inc", ->{=(this, "c", +(c, 1))}
  )),
  /* print(x), */
  print(x.gc),
  x.sc(4),
  print(x.gc),
  x.inc(),
  print(x.c)
)
